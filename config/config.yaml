defaults:
  - _self_
  - models
  - evaluation
  - induction
  - ablation
  - plotting
  # - override hydra/job_logging: disabled 
  - override hydra/hydra_logging: disabled 

# string identifier for wandb runs and generic I/O
run_name: null

seed: 42

hydra:
  output_subdir: null
  run:
    dir: logs

  job_logging:
    handlers:
      file:
        mode: w

# wandb parameters
wandb:
  log: false
  project_name: tsfm_lens
  entity: gilpinlab
  group_name: null
  resume: false
  resume_run_id: null
  tags: null